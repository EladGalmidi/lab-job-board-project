# BEFORE-STATE CAPTURE. This is the original run on minikube's default bridge CNI,
# which accepts NetworkPolicy objects but does not enforce them - hence
# 'CONNECTED (policy NOT enforced)' below. It is kept deliberately as the evidence
# for the finding in SOLUTION-k8s.md Task 2.4.
# The AFTER capture, on the rebuilt --cni=calico cluster where the policy genuinely
# blocks unauthorised pods, is in evidence/21-networkpolicy-enforced.txt.

===== K8s Task 2.4: NetworkPolicy =====
networkpolicy.networking.k8s.io/postgres-network-policy created

NAME                      POD-SELECTOR   AGE
postgres-network-policy   app=postgres   0s

--- which CNI is in use? ---
  no policy-capable CNI pod found (minikube default bridge CNI)
v1.35.1

--- TEST A: unauthorised pod -> postgres:5432 (policy says DENY) ---
RESULT: CONNECTED (policy NOT enforced)

--- TEST B: jobs-service pod -> postgres:5432 (policy says ALLOW) ---
Defaulted container "jobs-service" out of: jobs-service, wait-for-postgres (init)
RESULT: CONNECTED (as intended)
