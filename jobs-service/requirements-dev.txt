# Development and CI dependencies. Kept out of requirements.txt so they never
# reach the runtime image (jobs-service/.dockerignore also excludes tests/).
-r requirements.txt

pytest==8.3.3
httpx==0.27.2          # required by fastapi.testclient.TestClient
ruff==0.7.4            # linter used by the lint-test-python CI job
